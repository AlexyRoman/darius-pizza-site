# Ensure optional dependencies are installed
optional=true

# Use npm's legacy peer deps behavior for better compatibility
legacy-peer-deps=false

# Ensure native binaries are properly handled
fund=false
audit=false
