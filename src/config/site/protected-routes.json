{
  "routes": ["/dashboard"],
  "description": "List of routes that require password authentication. Routes should be specified without locale prefix (e.g., '/dashboard' not '/en/dashboard')",
  "credentials": {
    "note": "Username and password are configured via PAGE_ACCESS_USERNAME and PAGE_ACCESS_PASSWORD environment variables in .env.local",
    "example": "PAGE_ACCESS_USERNAME=admin\nPAGE_ACCESS_PASSWORD=your-secure-password-here",
    "location": ".env.local (not tracked in git for security)"
  }
}
